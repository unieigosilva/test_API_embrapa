version: '3.8'

services:
  web:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ../:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
